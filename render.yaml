services:
  - type: web
    name: omnilearn-lms
    runtime: node
    rootDir: server
    buildCommand: npm install --include=dev && npx tsc
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
